#application.yml 이 test 의 resources 에 있고 아무 설정도 없으면 default 로 밑의 주석처리된 설정으로 데이터베이스 실행
#spring:
#  datasource:
#    url: jdbc:h2:mem:test #메모리 모드로 동작
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop
logging:
  level:
    org.hibernate.SQL: debug # 로거를 통해 실행되는 쿼리 출력
    org.hibernate.type: trace # '?'로 나오는 쿼리파라미터에 어떤게 바인딩 되는지 로그로 찍어줌